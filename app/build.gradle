plugins {
    id "com.android.application"
}

android {
    namespace "top.nemodesk.qingjiuapi"
    compileSdk 35

    defaultConfig {
        applicationId "top.nemodesk.qingjiuapi"
        minSdk 23
        targetSdk 35
        versionCode 1
        versionName "1.0.0"
    }

    buildTypes {
        release {
            signingConfig signingConfigs.debug
            minifyEnabled false
        }
    }
}
